## 变更摘要


## 关联 Issue
Close #

## 自检清单
- [ ] `bash backend/tests/integration.sh` 全过
- [ ] `cd frontend && npx tsc --noEmit` 无错
- [ ] 改动了 schema → 已加 alembic migration
- [ ] 改动了 API → 已更新 `docs/` 或 README
- [ ] 不包含商业化代码 / 不引入需付费的依赖

## 截图（前端改动）
